[tox]
env_list =
    py3{10,11,12,13}
minversion = 4.6.0
isolated_build = True

[testenv]
description = run the tests with pytest
package = wheel
wheel_build_env = .pkg

commands =
    uv run -- pytest {tty:--color=yes} {posargs}
